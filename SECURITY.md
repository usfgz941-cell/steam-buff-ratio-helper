# Security & Privacy

这个项目是个人挂刀辅助工具，不应获取或保存 Steam / BUFF 的敏感账户凭据。

## 不需要提供的内容

任何排错或更新都 **不需要**：

- Steam 密码；
- Steam Guard 恢复码；
- Steam Cookie；
- Steam API Key；
- BUFF 登录 Cookie；
- 账户验证码。

如果有人以维护脚本为由索要这些内容，不要提供。

## 本地保存的数据

脚本会在 `steamcommunity.com` 对应的浏览器本地存储中保存：

- 导入的 BUFF 购买记录；
- 成本记录“已售”状态；
- 当前 Steam asset 与手动选择成本的本地映射；
- 用户设置；
- 短期市场缓存；
- 最近一次汇率缓存。

这些内容用于本地匹配和计算。

## 网络请求

脚本可能访问：

1. Steam Community 自身市场接口，用于：
   - 当前卖盘；
   - 近期成交历史；
2. 乌克兰国家银行 NBU 官方汇率接口，用于 CNY→UAH 换算。

脚本不应把 BUFF CSV 上传到第三方服务器。

## 不自动执行交易

脚本只辅助：

- 显示比例；
- 显示底线；
- 给出推荐价；
- 点击推荐卡片时填入“买家支付”。

脚本不会：

- 自动勾选 Steam 订户协议；
- 自动点击最终确认出售；
- 自动完成 Steam 手机确认。

## 公开仓库注意事项

本仓库是公开的，因此禁止提交：

- BUFF 个人 CSV；
- Steam 账户标识与完整交易记录的私人备份；
- Cookie / Token / API Key；
- 包含敏感数据的调试日志。

`.gitignore` 会屏蔽常见购买记录导出文件，但它不是安全边界；提交前仍应人工检查。

## 报告问题

排错时优先提供：

- 脚本版本；
- Steam 出售窗口截图；
- 挂刀助手面板截图；
- Console 中 `[挂刀助手]` 的错误；
- 一两条脱敏后的 CSV 样例（只有在解析问题时需要）。

不要公开完整个人 CSV。
