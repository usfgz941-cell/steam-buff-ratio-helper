# Changelog

## v0.1.3 — 2026-08-25

### 紧急恢复

- 回滚 v0.1.2 的新 payload 发布格式，恢复此前已验证可加载的 7 分片核心 payload。
- 保留 v0.1.1 的 SHA-256 完整性校验、本地已验证源码缓存、网络重试与指数退避。
- 原因：v0.1.2 切换为 `payload/v0.1.2/source.gz.b64` 后出现用户侧“脚本加载不出来”的实机故障；同时发现仓库 `verify-dist.mjs` 仍按旧 `PARTS` 分片格式校验，说明 v0.1.2 发布链路没有覆盖新格式。
- 为避免继续扩大故障面，先恢复稳定核心，再单独重做 v0.1.2 的市场底线 / SteamID 隔离 / 精确手续费功能。

> 注意：Tampermonkey 安装器版本为 v0.1.3，但临时回滚后的面板核心版本仍会显示 v0.1.0，这是本次恢复策略的预期结果。

## v0.1.2 — 2026-08-25

### 基于实机截图修正

- 当比例底线对应的最低可接受售价明显高于当前市场时，不再把“快速 / 均衡 / 耐心”三档都强行显示成同一个底线价。
- 新增“当前市场不满足比例底线”状态：显示市场参考价、最低可接受价、溢价百分比，并明确建议“暂不上架 / 等待市场变化”。
- 以“新星 | 香肠地狱”实机样本验证：当前市场约 ₴12–13，而 0.70 底线要求约 ₴25.23，这种场景应判定为不可正常推荐，而不是“快速 ₴25.23”。

### 费用与账户匹配

- 手续费预估优先使用 Steam 页面自己的 `GetItemPriceFromTotal` / `GetTotalWithFees`，仅在不可用时才回退到旧估算。
- BUFF 成本匹配增加当前 SteamID 隔离，避免多 Steam 账户之间串成本。
- 资产到成本记录的本地 assignment 也按 SteamID 分区，并兼容旧 assignment 数据。

### UI

- 将“成本约 / 到账约”的跨币种辅助文字改为“BUFF 成本折合 / Steam 到账折合”，减少把参考换算误认为真实结算金额的歧义。
- 核心 payload 与安装器统一升级到 v0.1.2。

## v0.1.1 — 2026-08-24

### 运行可靠性

- 过渡加载器增加已校验源码缓存。
- payload 首次通过 SHA-256 后写入 Tampermonkey 存储；后续页面优先使用缓存。
- GitHub Raw 暂时不可用时，可继续使用已经通过完整性校验的本地缓存。
- 分片请求增加有限次数重试与指数退避。
- 避免同一会话内反复弹出同一个启动错误。

### 工程与文档

- 新增 GitHub Actions 自动验证。
- 新增发行包校验脚本：分片数量、gzip 解压、SHA-256、JavaScript 语法和隐私扫描。
- 新增外部方案调研与架构缺口审计。
- 明确安装器版本与核心功能版本的区别。
- 明确下一阶段 P0：Steam 钱包费率元数据、单文件构建、真实 UAH 验收。

> 当前核心 payload 仍为 v0.1.0；v0.1.1 主要是安装器和工程可靠性更新。

## v0.1.0 — 2026-08-24

- 首版。
- 支持导入 / 更新 BUFF 购买记录 CSV。
- Steam 出售窗口自动匹配实际买入成本。
- 根据 Steam “您收款”实时计算实际挂刀比例。
- 默认 `0.70` 比例底线，可调整。
- 加入快速 / 均衡 / 耐心三档推荐挂价。
- 加入 FIFO 成本匹配、手动切换成本和已售标记。
- 使用 NBU CNY→UAH 汇率并支持手动汇率。
- 安装脚本加入 GitHub 自动更新地址与完整性校验。
